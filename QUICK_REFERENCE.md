# Quick Reference - دليل سريع\n\n## 🚀 تشغيل الموقع\n\n```bash\n# التثبيت (لو أول مرة)\ncd \"c:\\Users\\T.B\\Desktop\\فريندز\"\nnpm install\n\n# التشغيل\nnode server.js\n\n# الفتح\nhttp://localhost:3000\n```\n\n---\n\n## 📋 أساسيات الأمان\n\n### البيانات المهمة:\n- `data/users.json` - حسابات المستخدمين (محفوظة بآمان)\n- `data/products.json` - المنتجات\n- `data/orders.json` - الطلبات\n\n### المفاتيح:\n```javascript\n// في server.js:\nconst JWT_SECRET = \"friends_dev_secret_change_me\";  // ⚠️ غيّره!\nconst TOKEN_EXPIRES = \"7d\";                         // صلاحية التوكن\n```\n\n### Firebase:\n```javascript\n// في js/firebase-config.js:\nconst firebaseConfig = {\n  apiKey: \"...\",\n  authDomain: \"...\",\n  projectId: \"...\",\n  // ⚠️ استبدل البيانات بتفاصيل مشروعك\n};\n```\n\n---\n\n## 🔐 أوضاع المصادقة\n\n### 1️⃣ البريد الإلكتروني\n```\nURL: http://localhost:3000/auth.html\nBريد: test@example.com\nكلمة المرور: Test123456 (6+ chars)\n→ تسجيل الدخول مباشرة\n```\n\n### 2️⃣ رقم الهاتف\n```\nURL: http://localhost:3000/auth.html\nالهاتف: 010xxxxxxxxx أو +201xxxxxxxxx\n→ اضغط \"أرسل رمز التعبئة\"\n→ ادخل رمز (Firebase سيرسله)\n→ تسجيل الدخول\n```\n\n### 3️⃣ Google\n```\nURL: http://localhost:3000/auth.html\n→ اضغط \"دخول بـ Google\"\n→ اختر حسابك\n→ تم الدخول!\n```\n\n### 4️⃣ Facebook\n```\nURL: http://localhost:3000/auth.html\n→ اضغط \"دخول بـ Facebook\"\n→ ادخل بيانات Facebook\n→ تم الدخول!\n```\n\n---\n\n## 🔍 البحث والتصفية\n\n### البحث:\n```\n1. اكتب في شريط البحث\n2. اختر من الاقتراحات\n3. أو اضغط Enter\n4. النتائج تظهر فورًا\n```\n\n### الفلاتر:\n```\n1. اضغط زر \"فلاتر\"\n2. اختر:\n   - القسم\n   - السعر (min ← → max)\n   - التقييم (⭐)\n3. اضغط \"تطبيق\"\n4. النتائج تحدثت\n```\n\n### مثال مركب:\n```\nفلتر: عناية بالبشرة + 100-200 + 4⭐\nبحث: كريم\n→ نتائج: كريم ترطيب (4.7⭐) بـ 150 ج.م\n```\n\n---\n\n## 📍 الملفات الرئيسية\n\n### Backend:\n```\nserver.js                    خادم Express رئيسي\n├─ /api/auth/*             endpoints المصادقة\n├─ /api/products            المنتجات\n├─ /api/orders              الطلبات\n└─ /api/orders/my           طلبات المستخدم\n\ndata/\n├─ users.json              المستخدمين\n├─ products.json            المنتجات\n├─ orders.json              الطلبات\n└─ ...\n```\n\n### Frontend:\n```\nindex.html                 الصفحة الرئيسية\nauth.html                  صفحة المصادقة\ncart.html                  السلة\nproduct.html               المنتج\ncategory.html              القسم\n\njs/\n├─ app.js                 منطق الصفحة (بحث، فلاتر)\n├─ auth.js                مصادقة (email، phone، oauth)\n└─ firebase-config.js     Firebase settings\n\ncss/\n├─ style.css              أنماط أساسية\n└─ auth-search.css        أنماط جديدة\n```\n\n---\n\n## 🛡️ المتغيرات البيئية\n\n### يمكن تعديلها في `server.js`:\n\n```javascript\n// PORT\nconst PORT = process.env.PORT || 3000;\n\n// JWT Secret (⚠️ غيّره في الإنتاج!)\nconst JWT_SECRET = process.env.JWT_SECRET || \"friends_dev_secret_change_me\";\n\n// صلاحية التوكن\nconst TOKEN_EXPIRES = \"7d\";\n\n// كلمات المرور الافتراضية (للـ seed)\nconst baseHash = await bcrypt.hash(\"admin123\", 10);\n\n// أرقام الواتس\nconst ADMIN_WHATSAPP = \"201094708407\";\n\n// الأكواد الخاصة بالخصم\nconst discountCodes = {\n  FRIENDS10: 0.1,   // 10%\n  WELCOME15: 0.15   // 15%\n};\n```\n\n---\n\n## 🧪 اختبار سريع\n\n### تسجيل حساب جديد:\n```bash\ncurl -X POST http://localhost:3000/api/auth/register \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"أحمد\",\n    \"email\": \"ahmed@test.com\",\n    \"password\": \"Secure123\"\n  }'\n```\n\n### تسجيل الدخول:\n```bash\ncurl -X POST http://localhost:3000/api/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"email\": \"ahmed@test.com\",\n    \"password\": \"Secure123\"\n  }'\n```\n\n### الحصول على البيانات الشخصية:\n```bash\ncurl -X GET http://localhost:3000/api/auth/me \\\n  -H \"Authorization: Bearer YOUR_TOKEN_HERE\"\n```\n\n### طلبات المستخدم:\n```bash\ncurl -X GET http://localhost:3000/api/orders/my \\\n  -H \"Authorization: Bearer YOUR_TOKEN_HERE\"\n```\n\n---\n\n## 🐛 حل المشاكل السريع\n\n### \"Cannot find module 'firebase'\"\n```bash\n→ npm install\n```\n\n### \"Port 3000 already in use\"\n```bash\n→ لقتل العملية:\n   netstat -ano | findstr 3000\n   taskkill /PID <PID> /F\n→ أو استخدم port آخر:\n   PORT=3001 node server.js\n```\n\n### \"Firebase is not defined\"\n```html\n→ تحقق من ترتيب scripts:\n   1. Firebase SDKs\n   2. firebase-config.js\n   3. auth.js\n```\n\n### \"Email already exists\"\n```\n→ استخدم بريد آخر\n→ أو احذف من data/users.json\n```\n\n### \"Invalid phone format\"\n```\n→ استخدم: +201012345678 أو 01012345678\n→ لا تستخدم: 00201012345678\n```\n\n---\n\n## 🎨 تخصيص الألوان\n\n### في `css/style.css`:\n```css\n:root {\n  --red: #d83a3a;           /* الأحمر الأساسي */\n  --blue: #1b4fb8;          /* الأزرق الأساسي */\n  --aqua: #3bb7b7;          /* الفيروز */\n  --mint: #e6f6f4;          /* الأخضر الفاتح */\n  --light: #f3f7fb;         /* الخلفية الفاتحة */\n  --ink: #0f1f3a;           /* النص الداكن */\n  --muted: #5e6e86;         /* النص الرمادي */\n}\n```\n\n---\n\n## 📱 ملاءمة الجوال\n\n### Breakpoints:\n```css\n/* Desktop: 1200px+ */\n.container { width: 92%; max-width: 1200px; }\n\n/* Tablet: 768-1200px */\n.auth-layout { grid-template-columns: auto; }\n\n/* Mobile: < 768px */\n.search-filters { grid-template-columns: 1fr; }\n```\n\n---\n\n## 📚 الملفات الإضافية\n\n| الملف | الوصف |\n|------|-------|\n| `README.md` | الأساسية |\n| `FEATURES.md` | الميزات بالتفصيل |\n| `FIREBASE_SETUP.md` | خطوات Firebase |\n| `TESTING_CHECKLIST.md` | اختبار شامل |\n| `IMPLEMENTATION_SUMMARY.md` | ملخص التطوير |\n| `QUICK_REFERENCE.md` | هذا الملف 🎯 |\n\n---\n\n## ⚡ نصائح للإنتاج\n\n### قبل النشر:\n```bash\n# 1. اختبر كل شيء\n→ TESTING_CHECKLIST.md\n\n# 2. غيّر المفاتيح\n→ JWT_SECRET في server.js\n→ Firebase API keys معدة\n\n# 3. تأكد من البيانات\n→ لا توجد accounts اختبار\n→ ADMIN_WHATSAPP صحيح\n\n# 4. استخدم HTTPS\n→ Let's Encrypt أو Authority\n\n# 5. backup قاعدة البيانات\n→ احفظ data/ folder\n```\n\n### بعد النشر:\n```bash\n# 1. راقب الأخطاء\n→ Server logs\n→ Browser console\n\n# 2. تفقد الأداء\n→ Load times\n→ API response times\n\n# 3. أضف monitoring\n→ Tracking (analytics)\n→ Error reporting\n```\n\n---\n\n## 🆘 طلب المساعدة\n\n### خطوات quick debug:\n```\n1. افتح DevTools (F12)\n2. اذهب إلى Console\n3. ابحث عن أخطاء حمراء\n4. تفقد Network tab\n5. اختبر API مباشرة بـ curl\n```\n\n### معلومات مفيدة:\n- `localhost:3000/api/health` - فحص الخادم\n- `console.log()` في auth.js و app.js\n- Firebase Console > Logs\n- Browser DevTools Network\n\n---\n\n**آخر تحديث**: 26 فبراير، 2026\n